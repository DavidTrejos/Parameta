spring.application.name=employee-integration

server.port=8080

# --- Datasource ---
spring.datasource.url=jdbc:mysql://localhost:3306/parameta?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=12345

# --- JPA/Hibernate ---
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.jdbc.time_zone=UTC

# --- SOAP ---
soap.employee.endpoint-uri=http://localhost:8080/ws

